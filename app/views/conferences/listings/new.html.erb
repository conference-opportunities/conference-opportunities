<header>
  <h2>List your conference</h2>
</header>

<%= form_for(@conference_listing, url: conference_listing_path(conference_id: @conference_listing.id)) do |f| %>
  <section class="inputs">
    <fieldset>
      <%= f.label :name, 'Conference name' %>
      <%= f.text_field :name %>

      <%= f.label :website_url, 'Website' %>
      <%= f.text_field :website_url %>

      <%= f.label :twitter_handle, 'Twitter handle' %>
      <%= f.text_field :twitter_handle, disabled: true %>
    </fieldset>

    <figure>
      <%= f.hidden_field :logo_url %>
      <%= image_tag @conference_listing.logo_url %>
    </figure>
  </section>

  <section class="submit">
    <%= f.submit "Create listing" %>
  </section>
<% end %>
